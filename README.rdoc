= Bowling Game

This is an example of a Rails 4.0.0 app

=== Install
* bundle install

=== Run
* rake db:migrate
* rails s
* visit localhost:3000

=== Tests
* rake db:migrate
* rake db:test:prepare
* bundle exec rspec
